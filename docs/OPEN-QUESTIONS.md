# Open Questions

1. Does the scalar generalize to accents not in the training set?
2. How sensitive is the probe to the choice of WavLM layer weighting initialization?
3. Would a multi-task objective (WER regression + accent classification) improve routing?
4. What is the optimal escalation budget for production deployment?
